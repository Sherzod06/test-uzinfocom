export interface IProduct {
    model: string,
    released_date: number,
    category: string,
    price: string
    description?: string,
    created_at: string,
    is_visible: boolean,
    image?: string
}